[project]
name = "news-aggregator"
version = "0.1.0"
description = "Daily news feed aggregator with Japanese summaries"
requires-python = ">=3.12"
dependencies = [
  "feedparser>=6.0",
  "httpx>=0.28",
  "jinja2>=3.1",
  "pyyaml>=6.0",
]
