/** Pixel gap rendered between article cards (as padding-bottom on the
 *  virtualizer row wrapper). Shared between the view (`ArticleList`) and
 *  the controller hook so both agree on the same row stride. */
export const CARD_GAP: number = 16
